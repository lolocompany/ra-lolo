import React from 'react';
import { Edit, SimpleForm, TextInput } from 'react-admin';

const {{pascalCase resourceName}}Edit = (props) => (
  <Edit {...props}>
    <SimpleForm>
      <TextInput source="name" />
    </SimpleForm>
  </Edit>
);

export default {{pascalCase resourceName}}Edit;