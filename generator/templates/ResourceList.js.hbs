import React from 'react'; import { List, Datagrid,
{{#each components}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}
} from 'react-admin'; const
{{resourceName}}List = (props) => (
<List {...props}>
  <Datagrid>
    {{#each datagridComponents}}
      {{{this}}}
    {{/each}}
  </Datagrid>
</List>
); export default
{{resourceName}}List;