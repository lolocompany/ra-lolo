import React from 'react';

const {{resourceName}}List = (props) => (
<List {...props}>
  <Datagrid>
    {{#each components.listView}}
      {{{this}}}
    {{/each}}
  </Datagrid>
</List>
);

export default {{resourceName}}List;